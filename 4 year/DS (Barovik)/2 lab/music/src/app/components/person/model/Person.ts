import {Band} from '../../band/model/Band';

export class Person {
  id: number;
  band: Band;
  name: string;
  surname: string;
  birthday: Date;
  financialCondition: number;
  biography: number;
}
